function scrollToSection(issue) {
    var element = document.getElementById(issue);
    element.scrollIntoView({behavior: "smooth"});
}

function scrollToSection(info) {
    var element = document.getElementById(info);
    element.scrollIntoView({behavior: "smooth"});
}

function scrollToSection(testi) {
    var element = document.getElementById(testi);
    element.scrollIntoView({behavior: "smooth"});
}

function scrollToSection(info) {
    var element = document.getElementById(info);
    element.scrollIntoView({behavior: "smooth"});
}

function scrollToSection(prob) {
    var element = document.getElementById(prob);
    element.scrollIntoView({behavior: "smooth"});
}
function scrollToSection(abouts) {
    var element = document.getElementById(abouts);
    element.scrollIntoView({behavior: "smooth"});
}